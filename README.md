# HelloApp
This use case implements the most basic functionality of the HelloApp application. The program displays a simple greeting message "Hello World" in the console. It serves as the starting point for verifying that the Java environment and project setup are working correctly.
